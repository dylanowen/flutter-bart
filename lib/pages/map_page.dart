import 'package:flutter/material.dart';

class MapPage extends StatelessWidget {

  const MapPage();

  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Image.asset('assets/images/bart_map.png')
    );
  }
}